POSTGRES_USER=diaria
POSTGRES_PASSWORD=change_me
POSTGRES_DB=diaria_dev

# pgAdmin credentials
PGADMIN_DEFAULT_EMAIL=admin@diaria.local
PGADMIN_DEFAULT_PASSWORD=change_me_too

# Opcional: porta e outros valores podem ser configurados no .env local
# Exemplo de uso: copie este arquivo para `.env` e ajuste os valores.
